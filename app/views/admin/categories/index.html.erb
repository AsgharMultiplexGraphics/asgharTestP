<%# <p style="color: green"><%= notice</p> %>
<% if user_signed_in? %>
  <% if current_user.role != "cashier"%>
    <h1>Categories</h1>
    <div id="admin_categories">
      <%# <% @admin_categories.each do |admin_category| %>
        <%# <%= render admin_category %>
        <%# <p>
      <%= link_to "Show this category", admin_category , class:"btn btn-primary"%>
        <%# </p>  %>
        <%# <% end %>
      </div>
      <%= link_to "New category", new_admin_category_path , class:"btn btn-primary" %>
      <table class="table table-striped table-hover table-bordered" >
        <thead class="table-light">
          <tr>
            <th> ID</th>
            <th>Photo <%="\u{1F468}"%> </th>
            <th>Name <%="\u{1F468}"%> </th>
            <%# <th>Last name</th> %>
            <th>Description <%="\u{2709}"%> </th>
            <th>Added By <%="\u{1F60E}"%> </th>
            <th>Action <%="\u{2764}"%> </th>
            <% if current_user.email == "asghar@gmail.com"%>
              <th>Sr.No </th>
            </tr>
          </thead>
          <tbody>
            <div id="category">
              <%no= 0%>
              <% User.all.each do |user| %>
                <% user.categories.each do |category| %>
                  <%no += 1%>
                  <%# <% if friend.user == current_user %>
                  <%= render category %>
                  <td >
                    <%# <%=  link_to "Show ", friend, class: "btn btn-outline-dark" %>
                    <%# <button   class=""></button>   %>
                    <%= button_to "Delete",[:admin, category], method: :delete, class:"btn btn-outline-danger btn-sm", onclick: 'return confirm("Are you sure?");' %>
                    <%# <%= link_to "Edit", edit_friend_path(@friend),class:"btn btn-primary",style:"margin-top:10px; margin-bottom:10px;display:inline-block;" %>
                    <%# <%= friend.user_id %>
                  </td>
                  <td>
                    <%=no%>
                  </td>
                </tr>
                <%# <% end %>
              <% end %>
            <% end %>
          <% else %>
          </tr>
        </thead>
        <div id="category">
          <%# <% @admin_categories.each do |category| %>
            <%# <% if category.user == current_user %>
            <% User.all.each do |user| %>
              <% user.categories.each do |category| %>
                <%= render category %>
                <td >
                  <%# <%=  link_to "Show ", friend, class: "btn btn-outline-dark" %>
                  <%# <button   class=""></button>   %>
                  <%= button_to "Delete", [:admin, category], method: :delete, class:"btn btn-outline-danger btn-sm", onclick: 'return confirm("Are you sure?");' %>
                  <%# <%= link_to "Edit", edit_friend_path(@friend),class:"btn btn-primary",style:"margin-top:10px; margin-bottom:10px;display:inline-block;" %>
                  <%# <%= friend.user_id %>
                </td>
              </tr>
            <% end %>
          <% end %>
        <% end %>
      <% else %>
        <h1>You are not authorize to access this option</h1>
      <% end %>
    <% end %>
  </div>
</tbody>
</table>
