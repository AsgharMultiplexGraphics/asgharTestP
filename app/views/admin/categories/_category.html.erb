<div id="<%= dom_id category %>">
  <%# <p>
    <strong>Name:</strong>
    <%= category.name %>
  <%# </p>  %>
  <%# <p>
    <strong>Description:</strong>
    <%= category.description %>
  <%# </p>  %>
  <tr>
    <td ><%= category.id%></td>
    <%# <td><%= friend.user_id</td>  %>
    <td class="px-6 py-4 whitepsace-nowrap tex-sm font-medium text-gray-800 underline">
      <%# <%= category.image.present? ? image_tag(category.image.variant(:thumb)) : image_tag("https://via.placeholder.com/50") %>
      <% if category.image.attached? %>
        <%= image_tag(category.image, style: "width: 50px; height: 50px; object-fit: cover;") %>
      <% else %>
        <%= image_tag("https://via.placeholder.com/50") %>
      <% end %>
    </td>
    <td> <%=  link_to "#{category.name} " , [:admin, category] %>
      <%# <%= friend.first_name %>
    </td>
    <%# <td> <%= friend.last_name</td>   %>
    <td> <%= category.description %></td>
    <% if category.user.present? %>
      <td><%= category.user.name %> <%=category.user.role.to_s%></td>
    <% else %>
      <td>No associated user</td>
    <% end %>
  </div>
