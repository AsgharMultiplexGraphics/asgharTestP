<% if user_signed_in? && current_user.admin? %>
  <h1 class="fs-1"> POS FOR RETAIL AND SALES MANAGMENT </h1>
<%else%>
  <nav class="<%= nav_classes %>">
    <div class="container-fluid">
      <% if user_signed_in? %>
        <% if current_user.admin? %>
          <%= link_to 'Dashboard', home_path, class: "navbar-brand"%>
        <% elsif !current_user.admin? %>
          <%= link_to 'Home', home_path, class: "navbar-brand"%>
        <% end%>
      <%else%>
        <%= link_to 'Point of Sale', home_path, class: "navbar-brand"%>
      <% end%>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to 'About Us', home_about_path, class: "nav-link"%>
          </li>
          <li class="nav-item">
            <%= link_to 'Customers', friends_path, class: "nav-link" %>
          </li>
          <% if !user_signed_in? %>
            <li class="nav-item flex space-x-10 pl-5 pr-5">
              <%= link_to 'Shop', home_path, class: "text-gray-500 py-2 rounded-md text-md font-medium" %>
            </li>
            <li class="nav-item">
              <%= link_to 'Sign Up',  new_user_registration_path, class: "nav-link"%>
            </li>
            <li class="nav-item">
              <%= link_to 'Sign In', new_user_session_path, class:"btn btn-primary"%>
            </li>
          </ul>
        </div>
        <div class="flex-shrink-0 flex items-center pl-5 pr-5">
          <%= link_to root_path do %>
            <%= image_tag 'logo2.png', class: 'block lg:hidden h-10 w-auto' %>
          <% end %>
          <%= link_to root_path do %>
            <%= image_tag 'logo2.png', class: 'hidden lg:block h-10 w-auto' %>
          <% end %>
        </div>
        <div class="hidden sm:flex sml:ml-6 items-center justify-center pl-5 pr-5">
          <div class="flex space-x-10">
            <%= link_to cart_path, class: "text-gray-500 hover:underline py-2 rounded-md text-md font-medium" do %>
              <%# <%= icon 'fa-solid', 'shopping-cart' %>
              <span class = "fas fa-shopping-cart "> </span>
            <% end %>
          </div>
        <% end %>
        <% if user_signed_in? %>
        </li>
        <%# <li class="nav-item">
                  <%= link_to 'Add Customer', new_friend_path, class: "nav-link"%>
        <%# </li> %>
        <% if current_user.manager? %>
          <li class="nav-item">
            <%= link_to 'Categories', admin_categories_path, class: "nav-link"%>
          </li>
          <li class="nav-item">
            <%= link_to 'Products', admin_products_path, class: "nav-link"%>
          </li>
          <li class="nav-item">
            <%= link_to 'Orders',  admin_orders_path, class: "nav-link"%>
          </li>
        <% end %>
        <% if !current_user.admin? %>
          <li class="nav-item" style="margin-left:10px">
            <%= link_to "Profile' #{current_user.id} \u{2740}   #{current_user.email} \u{2740}", root_path, class:"nav-item btn btn-outline-info" %>
          </li>
        <% end%>
        <li  style="margin-left:10px">
          <%# <%= link_to 'Sign Out', destroy_user_session_path, method: :delete%>
          <%= button_to "Sign Out", destroy_user_session_path, :method => 'delete', data:{turbo: "false"}, class:"fas fa-sign-out-alt btn btn-primary mt-1 py-1", style: "color: #157347" %>
        </li>
        <form class="d-flex" style="margin-left:10px">
          <button class="btn btn-success"><%= link_to "Role: #{current_user.role.capitalize} \u{1F466}"%></button>
        </form>
      <% end %>
    </ul>
  </div>
</div>
</nav>
<% end %>
