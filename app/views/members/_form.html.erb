<% 

 url, method = if member.new_record?
   [members_path, 'post']
 else 
    [member_path(member,'put')]
 end 


 %>


    <%= form_with(model: member, url: url, method: method, local:true) do |form| %>
      <% if member.errors.any? %>
        <div style="color: red">
          <h2><%= pluralize(member.errors.count, "error") %> prohibited this member from being saved:</h2>

          <ul>
            <% member.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

    <div style= "margin-bottom:20px ;">
      <div class="field form-group" >
        <%# <%= form.label :first_name, style: "display: block" %> 
        
        <%= form.text_field :first_name, placeholder: "First Name" %>
      </div>

      <div class="field form-group">
        <%# <%= form.label :last_name, style: "display: block" %> 
        <br>
        <%= form.text_field :last_name, placeholder: "Last Name" %>
      </div>

      <div class="field form-group">
        <%= form.label :email, style: "display: block" %>
        <%= form.text_field :email %>
      </div>

      <div class="field form-group">
        <%= form.label :password, style: "display: block" %>
        <%= form.text_field :password %>
      </div>

      <div class="field form-group">
        <%= form.label :age, style: "display: block" %>
        <%= form.text_field :age %>
      </div>

      <div class="field form-group">
        <%= form.label :gender, style: "display: block" %>
        <%= form.text_field :gender %>
      </div>

 <div class="field form-group">
        <%= form.label :role, style: "display: block" %>
        <%= form.select :role, options_for_select(User.roles.keys, member.role) %>
      </div>

      <br/>
    
      <%# <%= current_user.inspect %>
    </div>

      <div class= "actions" style="margin-left:45px;">
        <%= form.submit class: "btn btn-primary" %>
      </div>
    <% end %>
